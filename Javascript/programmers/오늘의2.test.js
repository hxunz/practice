/**
* ## 이해
*
* 미지의 것: 해당 문자열에 포함된 모음의 순서를 뒤집어라
* 자료
*   - 모음은 소문자, 대문자 둘 다 가능하다.
* 조건
*   -
*
* ## 계획
*   -
*
* ## 실행
*
* ## 반성
*
*/

const solution = (str) => {

};

test('solution', () => {
  expect(solution('bucketplace')).toEqual('beckatplecu');
});