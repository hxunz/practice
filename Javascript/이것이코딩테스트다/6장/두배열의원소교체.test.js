const solution = (N, K, A, B) => {
  // 배열 A의 최소값을 구한다
  // 배열 B의 최대값을 구한다
  // 서로 바꾸고 count + 1을 해준다
  // count 값이 K가 될때까지 반복한다.
};

test('sumArr', () => {
  expect(solution(5, 3, [1, 2, 5, 4, 3], [5, 5, 6, 6, 5])).toEqual(26);
});
